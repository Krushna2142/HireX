version: '3.8'

services:
  ts-api:
    build: .
    ports:
      - "3001:3001"
    environment:
      - DATABASE_URL=postgresql://user:password@localhost:5432/dbname
    # volumes, restart, etc, optional