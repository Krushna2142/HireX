datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client-js"
}

model Resume {
  id          String   @id @default(uuid())
  userId      String
  fileName    String
  fileUrl     String
  parsedText  String?
  status      String
  createdAt   DateTime @default(now())
}