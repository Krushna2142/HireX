version: '3.8'
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-v*kY2a&GQr7v%Vf}
      POSTGRES_DB: ${POSTGRES_DB:-postgres}
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - '5432:5432'

  python-api:
    build: ./python-api
    depends_on:
      - postgres
    environment:
      - DATABASE_URL=postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-v*kY2a&GQr7v%Vf}@postgres:5432/${POSTGRES_DB:-postgres}
    ports:
      - '8000:8000'
    volumes:
      - ./python-api:/app  # For dev hot-reload

volumes:
  pgdata: