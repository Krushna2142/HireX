version: '3.8'

services:
  python-api:
    build: ./python-api
    env_file:
      - .env  # Add this to load your Supabase DATABASE_URL
    environment:
      - USE_LOCAL_SLM=false
    ports:
      - '8000:8000'
    volumes:
      - ./python-api:/app