services:
  python-ai-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: jobcrawler-python-ai-service
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
